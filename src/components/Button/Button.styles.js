import styled from 'styled-components';

export const BaseButton = styled.button`
  border: none;
  font: inherit;
  color: inherit;
  cursor: pointer;
  background: none;
`
export const NavbarToggleButton = styled(BaseButton)`
    color: var(--white);
    font-size: 30px;
    padding: 5px;
`